import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    backgroundColor: 'white',
    padding: 5,
    margin: 30,
  },
  title: {
    fontSize: 21,
    textAlign: 'center',
  },
});
